# Claude Code Instructions
このプロジェクトは暗号資産トレード戦略分析アプリ「trading-app-v2」開発のプロジェクトです。
btc-strategy（既存の本番運用アプリ）の基本ルール・技術構成を踏襲しつつ、アイデアベースから新規に構築します。

## プロジェクトの優先事項
- トレード判定ロジックを信頼性の高いものとし本番運用できるようにすること
- そのためのバックテスト、フォワードテスト、検証機能の精度をあげること

## 開発の方針
- 日本語で回答する。途中で英語になることがあるため気を付ける
- セキュリティを優先する
- APIキーや .env.local は読まない
- 提案→計画→実装調査→実装→再チェック
- このリポジトリ内のリソースへのアクセスはすべて許可します
- 検証・分析⇒修正⇒テスト⇒デバッグのサイクルを開発件名をもとに回す

## 運営体制（マルチエージェント）
- 本プロジェクトの開発・分析は**6体のマルチエージェント体制**で進める。正式な定義・フロー・役割境界は `obs/trading_app/00プロジェクト方針/PJ000002-マルチエージェント運営計画書.md` を正とする（本項は索引＋要点）。
- **構成**: 司令塔（ユーザー）＋ 実験ライン5体（設計チーム→実装チーム→品質チーム→デプロイチーム→進行チーム）＋ その上位に立つ戦略チーム。各エージェント定義は `.claude/agents/*.md`。
  - S 戦略チーム(chief-strategist): クリプト市場の目利き・全体監視・司令塔への具申
  - A 設計チーム(strategy-architect): 実験仕様(spec)確定。成功基準を**回す前に数値で**固定
  - B 実装チーム(quant-researcher): specに忠実に実装。予測単位＋パイプライン両方の**生データのみ**出力（解釈しない）
  - C 品質チーム(adversarial-reviewer): 好結果を壊しにいく偽陽性の最終防波堤。採用/不採用を宣告
  - D デプロイチーム(integration-deploy): 採用＋司令塔GOのものだけ本番反映
  - E 進行チーム(archivist-pm): OBS移動・リンク修正・引き継ぎ・ACTIVE更新
- **作業場**: `research/`（EXP-OBSxxxxx/＝実験1件=OBS1件のバケツリレー。ACTIVE.md=進行信号機、STRATEGY-BRIEF.md=作戦ブリーフ、portfolio-ledger.md=採用/不採用台帳、_templates/=雛形）。`research/`は揮発、`obs/`が記録の正。
- **鉄則**:
  - B(実装)とC(判定)は必ず別エージェントとし、自分の実装を甘く採点させない
  - 採用ゲートは人間。Cの「採用可」は推薦であり、本番反映(Dの起動)は**司令塔の最終GO**を必須とする
  - 不採用も負の結果としてPJ000001の変更履歴に残す（偽陽性回避の資産）
  - リレーは全自動ではない。オーケストレーター（Claude）が各サブエージェントを順に起動する運用
- **司令塔の必須操作は2点のみ**: ①Sにテーマを渡す ②Cの宣告に対し本番反映のGOを出す。状況把握は `research/ACTIVE.md` と `research/STRATEGY-BRIEF.md` の2枚で完結させる。

## コード修正方針
- 修正前に影響範囲を確認する
- あいまいな要求は、不明点を確認してからコード変更を実施
- 不具合があれば他にもどうような不備がないかチェックする
- 要求事項がわかりにくい場合は仕様提示して認識誤りがないことを確認
- 今後の保守メンテナンスを考慮したロジックとする
- 必要に応じてリファクタリングした方がよいタイミングがあれば自ら提案
- スマホで指示することが多いため、ユーザのオペレーションは極小とする。
- ブラウザの開発用コンソールの確認をユーザに求めるのではなく、アプリ固有のログ機能の拡張によりデバッグを進める方式とする

### プロジェクト進捗サマリの定期更新
- `obs/trading_app/00プロジェクト方針/PJ000003-プロジェクト進捗サマリ.md` を**週次（毎週）または重要判定の直後**に更新する。
- 更新内容: ゴールまでの距離（KPI現在地）・直近マイルストーン・進行中実験の状態・重要課題・次のアクション。
- セッション開始時に `research/ACTIVE.md` と `research/STRATEGY-BRIEF.md` を読んだ後、PJ000003も確認して現状把握を行う。
- 更新のトリガー（いずれかに該当したら更新）:
  - 週次の定例タイミング
  - C品質チームの採用/不採用宣告
  - フォワード較正の中間チェックポイント（30日/60日/90日到達）
  - 新しい実験（EXP-OBSxxxxx）の起票または完了
  - 司令塔から「進捗をまとめて」と依頼があったとき

### ドキュメント体系
## 基本方針
- 本アプリに関するドキュメントは、\docs 配下に保管する。
- 基本は、md形式とするが、ユーザが求められた場合はその形式で作成する。
- 行使する場合は、変更履歴を入れる。
- ドキュメントの更新タイミングは、1週間に1回とする。あるいはユーザから依頼があったとき。

## 作成単位
- ドキュメントが巨大にならないようにするため、章立て単位に1つのMDファイルとするイメージ
- 大きくは、以下の４つ
　A-利用者導入編
　B-トレーダ向けアプリ活用編
　C-トレーディング仕様解説編
　D-開発者向けシステム仕様編
- 上記の単位でフォルダを作成し、目次ファイル(例 A000-目次.md)を作成し各種リンクできるようにする。
- ドキュメントは、A001-ファイル名.md とする。

## 開発件名管理
### 管理体系
- \obs\trading_app\00プロジェクト方針　は、個別の開発件名（改善件名）とは別に、プロジェクト全体の方向性を定める方針ドキュメントを格納します。通常の開発件名ライフサイクル（アイデア→検討中→対応待ち→実装中→解決済）には乗せず、目標（ゴール）が達成されるまでクローズしません。内容の見直しは変更履歴に追記して継続更新します。
- \obs\trading_app\01開発アイデア　は、アイデアや提案事項のメモです。
- \obs\trading_app\02対応検討中　は、対応方法検討中のメモです。
- \obs\trading_app\70対応待ち 　は、現在対応している内容が完了したら実施予定のメモです。ToDoですね。
- \obs\trading_app\80実装中 実装中のものです。
- \obs\trading_app\90解決済 実装完了して解決したものです。
- \obs\trading_app\85レビュー は、外部（第三者AI・レビュアー等）から受け取ったレビュー・改善提案の原本や、プロジェクト全体の現状レビューを保管します。開発件名ライフサイクルとは別に、参考資料として保存する場所です。内容を正式採用する場合は、通常のOBS件名として別途起票します。

### 開発件名ファイルについて
- OBS999999-開発件名.md とします。
- 999999は、000001からのSEQとします。OBSは固定。ファイル名にこのSEQが無い場合は、付与してください。
- 初回は全てSEQがないため、ファイルのタイムスタンプの古いもの順で付与してください。
- すでに先頭にSEQらしき数字では事案る番号が付与されているファイル名は、ファイル名からその番号を削除して上記ルールで付与してください。

### 開発件名のメモの作成ルール
- ヘッダーに変更履歴を付与する。ルール適応後からで問題ないので過去ファイルは対応不要。
- MDファイルから派生して作成されたMDファイルは、リンクする。派生課題、関連課題など分類してリンクの紐づけを行う。
- ファイル名のリネーム、フォルダ移動によりリンクが切れるため再度リンクを張り直ししてください。

### 開発件名のメモの自動整理
- 件名のコード修正を開始したら、実装中フォルダにMDファイルを移動します。
- 課題があればや提案があれば、件名ファイルを新規作成し開発アイデアに保存する。
- 件名のコード修正を開始したら、実装中フォルダにMDファイルを移動します。
- 解決したら解決済みフォルダにMDファイルを移動します。
- それぞれ実施した日付のフォルダに入れていきます。
- 移動元のファイルは削除されてOK。移動元の日付フォルダもMDファイルがなくなれば削除。
- それぞれ作成、ステータス変更を行った日付のフォルダに入れていきます。
- 件名ファイルにて調査結果、対応案、解決方法などは常に件名ファイルをアップデート。

## セッション間引き継ぎ（\obs\trading_app\引き継ぎ）
### フォルダ構成
- \obs\trading_app\引き継ぎ\01未完了 　… 現在アクティブな引き継ぎ書。**次セッションで最初に読むべき「今どこにいて、次に何をするか」**を書く。原則1件（最新のもの）を置く。
- \obs\trading_app\引き継ぎ\02完了済み 　… 対応が完了・陳腐化した過去の引き継ぎ書の保管先。

### ファイル命名
- `YYYYMMDD-引き継ぎ.md` とする（1日に複数あれば `YYYYMMDD-用途-引き継ぎ.md`）。

### 運用ルール
- **セッション開始時**: まず `01未完了` の引き継ぎ書を読み、現状と次アクションをユーザに確認してから作業を始める。
- **セッション終了時 / 区切りの良いところ**: `01未完了` の引き継ぎ書を最新状態に更新する（到達点・次アクション・環境メモ）。内容が古くなった引き継ぎ書は `02完了済み` へ移動する。
- 引き継ぎ書には関連する OBS 件名へのリンクを張り、詳細は各 OBS 側に残す（引き継ぎ書は索引＋要約に徹して肥大化を防ぐ）。

## ビルド、リソース管理
- コード修正時は、リビルドとデプロイまで実施
- github で管理。一人開発のため、main ブランチにて直接コミット
- ビルドは、github auto にて実施

## プロジェクト間サイクル（crypto-strategy-lab → trading-app-v2 → btc-carry-executor）
- 2026-07-12、司令塔と3段階サイクルの方針を確認：
  1. **crypto-strategy-lab**（多角的戦略探索）: 複数の戦略ロジック系統（オンチェーン系・センチメント系・統計的裁定系・ボラティリティ系等）を横断的にprescreen〜Stage検証まで並行して回す実験室
  2. **trading-app-v2**（本プロジェクト・POC〜フォワード較正）: labで有望と判定された系統、または本プロジェクト独自の系統（現状は②モメンタム＋キャリー）を深掘りし、バックテスト〜フォワード較正まで仕上げる
  3. **btc-carry-executor**（本実装・実行層）: trading-app-v2側で司令塔GOが出た戦略のみ、実際の注文発行・ポジション管理を担う薄い実行層
- **各プロジェクトは独立運用**（別リポジトリ・別Firebaseプロジェクト、6体マルチエージェント体制も各プロジェクトでフル運用——crypto-strategy-labも横展開時の検証水準を落とさないため簡略化しない）
- **プロジェクト間の橋渡しは軽い一方向のみ**：crypto-strategy-labで有望となった系統をtrading-app-v2に引き継ぐ際は、リアルタイム同期やコード共有ではなく、**新規OBS番号を起票して手動で移植・書き起こす**。trading-app-v2→btc-carry-executorも同様に、戦略パラメータをC品質チーム承認後に手動転記する方式を維持する（クロスリポジトリでの自動同期は監視用ダッシュボード等の非戦略系データに限定し、戦略ロジック自体の自動同期は行わない）
- 「共通資産」置き場は先回りして作らない。重複が実際に問題になった時点で共通パッケージ化を検討する（各プロジェクトのCLAUDE.mdに共通の方針として明記済み）

## btc-strategyとの関係
- 本プロジェクトはbtc-strategyとは別のFirebaseプロジェクト・別リポジトリとして独立して運用する
- btc-strategyは本番運用中のため、本プロジェクトの都合でbtc-strategy側のコードに手を入れない
- 認証まわり（Googleログイン・許可メールアドレス制御）など再利用性の高い基盤コードはbtc-strategyを参考に移植している
- 将来的に両プロジェクトでロジックを共有する必要が生じた場合は、その時点で共通パッケージ化を検討する

---

## 技術スタック

### フロントエンド
- **言語・フレームワーク**: TypeScript + React 19 + React Router 7
- **ビルドツール**: Vite 7
- **状態管理**: TanStack React Query 5（サーバ状態）
- **UI・スタイル**: Tailwind CSS 3 + Lucide React（アイコン）
- **チャート**: Recharts 2（データ可視化）
- **バックエンド連携**: Firebase 12 + Claude API (`@anthropic-ai/sdk`)

### バックエンド・インフラ
- **認証**: Google Firebase Authentication
- **リアルタイムDB**: Firebase Realtime Database
- **クラウド関数**: Firebase Cloud Functions
- **推論エンジン**: Claude API（パターン認識・シグナル判定）

### 開発環境
- **Node.js**: ES Module対応
- **パッケージ管理**: npm
- **型チェック**: TypeScript strict mode
- **コード品質**: ESLint推奨（`.claude/settings.json` で許可制御）

---

## プロジェクト構造

```
trading-app-v2/
├── .claude/                        # Claude Code設定
│   ├── agents/                     # マルチエージェント指示書（6体）
│   │   ├── chief-strategist.md     # S: 戦略チーム
│   │   ├── strategy-architect.md   # A: 設計チーム
│   │   ├── quant-researcher.md     # B: 実装チーム
│   │   ├── adversarial-reviewer.md # C: 品質チーム
│   │   ├── integration-deploy.md   # D: デプロイチーム
│   │   └── archivist-pm.md         # E: 進行チーム
│   └── settings.json               # Claude Code環境設定
├── .github/                        # GitHub Actions
│   └── workflows/                  # 自動ビルド・デプロイ
├── docs/                           # ユーザ・開発者向けドキュメント
│   ├── 000-目次.md
│   ├── A-利用者導入編/
│   ├── B-トレーダ向けアプリ活用編/
│   ├── C-トレーディング仕様解説編/
│   └── D-開発者向けシステム仕様編/
├── functions/                      # Firebase Cloud Functions（バックエンド）
├── obs/                            # 永続記録（プロジェクト方針・引き継ぎ書）
│   └── trading_app/
│       ├── 00プロジェクト方針/     # PJ000001～PJ000003
│       ├── 01開発アイデア/
│       ├── 02対応検討中/
│       ├── 70対応待ち/
│       ├── 80実装中/
│       ├── 90解決済/
│       ├── 85レビュー/
│       └── 引き継ぎ/                # セッション間引き継ぎ書
├── research/                       # 実験作業場（揮発可）
│   ├── ACTIVE.md                   # E管理：進行状況信号機
│   ├── STRATEGY-BRIEF.md           # S管理：作戦ブリーフ
│   ├── portfolio-ledger.md         # S管理：採用/不採用台帳
│   ├── _templates/                 # 実験成果物の雛形
│   └── EXP-OBSxxxxx/               # 実験ごとのフォルダ
│       ├── 00-prescreen.md         # S: 目利きゲート
│       ├── 00-spec.md              # A: 実験仕様
│       ├── 10-result/              # B: 生データのみ
│       ├── 20-review.md            # C: 採用/不採用判定
│       └── 30-decision.md          # D/E: 反映決定・記録
├── scripts/                        # 検証・バックテストスクリプト
├── src/                            # 本番実装コード
│   ├── App.tsx                     # ルートコンポーネント
│   ├── main.tsx                    # エントリーポイント
│   ├── components/                 # UI コンポーネント
│   │   ├── screens/                # ページレベルコンポーネント
│   │   ├── shell/                  # アプリケーションシェル
│   │   └── [その他UI要素]/
│   ├── hooks/                      # React カスタムフック
│   │   └── useAuth.tsx             # Google認証フック
│   ├── types/                      # TypeScript型定義
│   ├── utils/                      # ユーティリティ関数
│   ├── lib/                        # 外部API連携（Firebase、Claude等）
│   ├── decision-layer/             # トレード判定レイヤー
│   ├── execution-layer/            # 発注実行レイヤー
│   ├── llm-layer/                  # Claude API連携層
│   ├── pattern-engine/             # パターン認識エンジン
│   ├── pipeline/                   # バックテストパイプライン
│   ├── risk-layer/                 # リスク管理レイヤー
│   ├── data/                       # 静的データ・定数
│   ├── shared/                     # 共有ユーティリティ
│   ├── ui/                         # 共有UI要素
│   └── index.css                   # グローバルスタイル
├── public/                         # 静的アセット
├── package.json                    # 依存関係
├── tsconfig.json                   # TypeScript設定
├── vite.config.ts                  # Vite設定
└── CLAUDE.md                       # このファイル

```

### src/内の主要モジュール
- **components/screens/**: ページコンポーネント（ホーム、パターン分析、シグナル、評価、フォワード較正等）
- **hooks/useAuth.tsx**: Google Firebase認証フック（許可メールアドレス制御含む）
- **decision-layer/**: トレード判定ロジック（エントリー・エグジット条件）
- **pattern-engine/**: パターン認識・フィルタリング
- **pipeline/**: バックテストの並列処理・集計
- **llm-layer/**: Claude API呼び出し（パターン相談・判定補助）
- **risk-layer/**: ポジションサイズ・損切り・利確ロジック

---

## 開発フロー

### ローカル開発
```bash
# 環境構築
npm install

# 開発サーバ起動（ホットリロード）
npm run dev

# 本番ビルド
npm run build

# ビルド確認
npm run preview
```

### コード修正の進め方
1. **影響範囲確認**: ファイル構造・型定義・パイプライン統合を確認
2. **修正前提示**: 要件があいまいな場合は仕様を確認
3. **実装**: TypeScript strict mode を遵守、型安全を保証
4. **テスト**: 関連機能（バックテスト・パターン分析等）で動作確認
5. **コミット**: 変更内容を簡潔に説明し、関連OBS件名をリンク

### ビルド・デプロイ
- **ビルド**: `npm run build` 後、バイナリをGitHub Actionsで自動テスト
- **デプロイ**: 本番反映は**D デプロイチーム (integration-deploy.md)** が担当
  - 既存バックテスト回帰確認は必須
  - Firebase設定・環境変数は `functions/` で一元管理
  - デプロイ完了後は新バージョンをトップページに表示

---

## キー設計原則

### 検証フロー
- **予測単位**（パターン単位のシグナル精度）と **パイプライン水準**（ポートフォリオレベルのバックテスト結果）の両方を測定
- 各実験の **生データを独立に保存** し、C品質チームが原データから再判定可能に
- 品質チーム（C）は実装チーム（B）と別エージェント → 自分の実装を甘く採点しない

### セキュリティ
- **APIキー・認証トークン**: `.env.local` は読まない。環境変数はGitHub Secretsで管理
- **許可メールアドレス**: Firebase認証後、`useAuth` フックで制御
- **コード公開**: このリポジトリは公開（秘密情報は入れない）

### 日本語対応
- コードコメント・ドキュメント・エラーメッセージは日本語を基本
- 技術用語は英語のまま（React、Firebase、Claude等）

---

## デバッグ・ログ管理

### アプリ固有のログ機能
- ブラウザコンソール確認ではなく、アプリ側でログパネルを拡張
- バックテスト・パターン分析の中間結果をアプリに表示
- ユーザがスマホで指示することが多いため、UI操作で確認・エクスポート可能に

### 本番デバッグ
- `import.meta.env.VITE_*` で埋め込まれたバージョン情報
  - `VITE_APP_VERSION`: package.json のバージョン
  - `VITE_GIT_COMMIT`: ビルド時のgit短縮コミットハッシュ
  - `VITE_DEPLOY_TIME`: ビルド時刻（日本時刻）

---

## テスト・検証

### バックテスト検証
- `scripts/` フォルダに検証スクリプトを配置
- 各実験ごとに **生データのみ** を `research/EXP-OBS*/10-result/` に出力
- パイプライン統合後、本番反映前に **既存バックテスト回帰テスト** を実施（D デプロイチームが担当）

### 型チェック・ビルド確認
```bash
npm run build  # TypeScript型チェック + Viteバンドル
```

---

## ドキュメント管理ルール

### ドキュメント体系（docs/）
- **A-利用者導入編**: セットアップ、初期設定、トラブルシューティング
- **B-トレーダ向けアプリ活用編**: UIガイド、シグナル見方、ポジション管理
- **C-トレーディング仕様解説編**: 戦略ルール、パターン定義、リスク管理
- **D-開発者向けシステム仕様編**: システムアーキテクチャ、API仕様、拡張ガイド

### ファイル命名
- `A001-ファイル名.md`, `B002-ファイル名.md` ... (章立てごとに通番)
- 更新履歴をヘッダーに記載
- リンク切れを防ぐため、ファイル移動時は派生・関連リンクを張り直し

### 更新周期
- 標準: 1週間に1回（金曜日推奨）
- トリガー: ユーザ依頼があった場合は即座に対応

---

## IDE・エディタ設定

### VSCode推奨拡張機能
- **ES7+ React/Redux/React-Native snippets**: React開発補助
- **TypeScript Vue Plugin**: TypeScript型チェック
- **Tailwind CSS IntelliSense**: Tailwindクラス補完
- **Firebase Explorer**: Firebase設定確認

### フォーマット・リント
- Prettier: コード自動整形（`.prettierrc` 推奨）
- ESLint: コード品質チェック
- TypeScript: strict mode で型安全を保証

---

## 進行状況の把握

### 必読ドキュメント（セッション開始時）
1. **`research/ACTIVE.md`**: 進行中の実験状況（誰が何をしているか）
2. **`research/STRATEGY-BRIEF.md`**: S 戦略チームの作戦提案（どこへ向かうか）
3. **`obs/trading_app/00プロジェクト方針/PJ000003-プロジェクト進捗サマリ.md`**: 全体KPI・課題・次アクション
4. **`obs/trading_app/引き継ぎ/01未完了/YYYYMMDD-引き継ぎ.md`**: 前セッションの到達点

### 定期更新タイミング
- **週次**: PJ000003 を金曜夕方に更新
- **イベント駆動**: 実験終了・品質チーム宣告・フォワード較正マイルストーン到達時

---

## FAQ & トラブルシューティング

### ビルドエラー
- `npm install` を実行し、`node_modules` を再構築
- `npm run build` で詳細なコンパイルエラーを確認
- `package.json` 依存関係が一致しているか確認

### Firebase接続エラー
- `src/lib/` の Firebase初期化ファイルで認証情報を確認
- Google Cloud Console で API 有効化状況を確認
- `.env.local` 等の秘密情報は使わない（GitHub Secrets推奨）

### パターン分析が遅い
- Claude API の トークン使用量・レート制限を確認
- バックテスト期間を短縮して検証
- `llm-layer/` の キャッシング機構を活用

### テスト環境とのデータ不一致
- `scripts/` で backtest data と forward calibration data を分離
- 予測単位とパイプライン両方の結果を確認
- 乖離があれば `research/EXP-OBS*/` に記録して品質チームへ報告
