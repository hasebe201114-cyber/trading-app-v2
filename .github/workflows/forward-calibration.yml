name: OBS000032 Forward Calibration (Daily)

on:
  schedule:
    # 毎日 01:00 UTC に実行（Bitget 00:00 UTC funding 確定後1時間のマージン）
    - cron: "0 1 * * *"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Set to 'true' to run without committing results"
        required: false
        default: "false"

jobs:
  calibrate:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    permissions:
      contents: write   # 結果をコミット・push するために必要

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # デプロイキーなし。GITHUB_TOKEN で push するためにここで fetch-depth を確保
          fetch-depth: 1

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Run forward calibration script
        env:
          # fapi.binance.com は GitHub Actions（米国IP）から451地理ブロックを受けるため、
          # 東京リージョンのCloud Functionsプロキシ（functions/index.js）経由でF2用funding dataを取得する。
          BINANCE_PROXY_URL: ${{ secrets.BINANCE_PROXY_URL }}
          BINANCE_PROXY_TOKEN: ${{ secrets.BINANCE_PROXY_TOKEN }}
        run: |
          node --experimental-strip-types scripts/bitget-carry-forward-paper.ts

      - name: Sync forward data to public folder (web app)
        run: |
          mkdir -p public/data/forward
          cp research/EXP-OBS000032/10-result/forward/forward-interim-metrics.json public/data/forward/
          cp research/EXP-OBS000032/10-result/forward/forward-paper-ledger-btc.json public/data/forward/
          cp research/EXP-OBS000032/10-result/forward/forward-paper-ledger-eth.json public/data/forward/

      - name: Commit and push results
        if: ${{ github.event.inputs.dry_run != 'true' }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # research/ と public/data/forward/ 両方を対象に変更チェック
          if git diff --quiet research/EXP-OBS000032/10-result/forward/ public/data/forward/; then
            echo "No changes to forward calibration results — skipping commit"
          else
            git add research/EXP-OBS000032/10-result/forward/ public/data/forward/
            LIVE_DAYS=$(node -e "
              const fs = require('fs');
              const path = 'research/EXP-OBS000032/10-result/forward/forward-interim-metrics.json';
              if (fs.existsSync(path)) {
                const d = JSON.parse(fs.readFileSync(path, 'utf-8'));
                const btc = d.liveDaysCount?.BTC ?? 0;
                const eth = d.liveDaysCount?.ETH ?? 0;
                console.log(Math.min(btc, eth));
              } else { console.log(0); }
            " 2>/dev/null || echo "?")
            git commit -m "chore(forward): daily calibration update — live day ${LIVE_DAYS}/90"

            # push前に git pull --rebase で競合を回避（複数同時実行対策）
            git pull --rebase origin main
            git push origin main
            echo "Committed and pushed forward calibration results (live day ${LIVE_DAYS}/90)"
          fi

      - name: Upload results as artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: forward-calibration-${{ github.run_id }}
          path: research/EXP-OBS000032/10-result/forward/
          retention-days: 30
