# セッション引き継ぎ書

**作成日**: 2026-07-10  
**ステータス**: 進行中  
**次の重要マイルストーン**: 2026-08-10（ドライラン1ヶ月到達・LIVE切替判断）

---

## 現在地（どこまで完了したか）

### trading-app-v2（Webアプリ）
| 項目 | 状態 |
|------|------|
| /forward 画面（フォワード較正モニター） | ✅ 本番反映済み（https://trading-app-v2-94de8.web.app/forward） |
| 累積収益率グラフのゼロ起点修正 | ✅ 完了（ライブ開始日を0基準に統一） |
| public/data/forward/ Day 6 手動同期 | ✅ 完了（本日プッシュ） |
| Day 7以降の自動sync（workflow） | ✅ forward-calibration.yml に組み込み済み |

### btc-carry-executor（自動売買実行層）
| 項目 | 状態 |
|------|------|
| Bitget API v1 → v2 全面移行 | ✅ 完了（bitget-client.ts 全書き換え） |
| execute-daily.ts 注文実行ロジック追加 | ✅ 完了（openDeltaNeutral / closeDeltaNeutral） |
| open-position.ts 修正（価格取得・coinQty計算） | ✅ 完了 |
| emergency-close.ts 新規作成 | ✅ 完了 |
| Discord通知（alert.ts） | ✅ 完了（crypto-discord-botと仕様統一） |
| GitHub Actions daily-run.yml | ✅ 完了（毎日 UTC 00:05 自動実行・ドライラン） |
| GitHub Actions monitor-margin.yml | ✅ 完了（4時間おき証拠金監視） |

### 90日フォワード較正（research/）
| 項目 | 状態 |
|------|------|
| Day 6 完了 | ✅ 2026-07-10T04:51 UTC |
| F1〜F4 全ゲート | ✅ 通過中（試験値フェーズ） |
| アラート | なし（alertActive=false） |
| F2 符号一致率 | ⚠️ null のまま（Binance API 地理ブロック問題・既知・未解決） |

---

## 次のアクション（優先順）

### 1️⃣ GitHub Secrets 設定（司令塔の操作）
`hasebe201114-cyber/btc-carry-executor` → Settings → Secrets → Actions に以下を登録：

| Secret名 | 内容 |
|---------|------|
| `BITGET_API_KEY` | Bitget APIキー |
| `BITGET_API_SECRET` | Bitget APIシークレット |
| `BITGET_PASSPHRASE` | Bitgetパスフレーズ |
| `DISCORD_WEBHOOK_DAILY` | crypto-discord-bot と同じWebhook URL |

登録後、GitHub Actions「OBS000032 Daily Signal Check」を手動実行（workflow_dispatch）して
Discordに通知が届くか確認する。

### 2️⃣ 毎日自動（Actions任せ）
- `trading-app-v2` → forward-calibration.yml が UTC 01:00 に自動実行
- `btc-carry-executor` → daily-run.yml が UTC 00:05 に自動実行（ドライラン）
- **人手不要**。Discordに通知が届く。

### 3️⃣ 2026-08-10（1ヶ月後）: LIVE切替判断
ドライラン1ヶ月を確認後、司令塔が判断。切替する場合：
```
btc-carry-executor の GitHub Actions で
LIVE_MODE=true、CAPITAL_USDT=500 を指定して workflow_dispatch
```
または `open-position.ts` を一度手動実行してポジション初回オープン。

### 4️⃣ 2026-10-02（Day 90）: F1〜F4 正式判定
- C品質チーム（adversarial-reviewer）を起動
- `forward-f1f4-verdict-inputs.json` が自動生成されてから実施

---

## 解決済みの既知問題

### F2 Binance 地理ブロック（451エラー）— 2026-07-11 解消
- **症状**: `eventSignAgreementPct = null`、F2判定が出ない
- **原因**: GitHub Actions（米国IP）から `fapi.binance.com` への直接アクセスが451地理ブロックされる（ローカル日本からは200で到達可能と確認済み）
- **対応**: Firebase Blazeプランへ切替（課金設定・予算アラート¥1,000/月を設定済み）。asia-northeast1にCloud Functions(2nd gen)の中継プロキシ `functions/binanceFundingProxy` を新設し共有トークンで保護。`scripts/bitget-carry-forward-paper.ts` を `BINANCE_PROXY_URL`/`BINANCE_PROXY_TOKEN`（GitHub Secretsに設定済み）経由でBinance funding rateを取得するよう修正（未設定時は従来通り直接アクセス＝ローカル実行は無変更）。
- **確認結果**: GitHub Actions実運用環境でBTC/ETHとも22件のfunding取得に成功、F2イベントペア一致率81.8%（閾値80%達成・試験値）を記録。コミット `7b22f99`。
- **運用コスト**: Cloud Functions呼び出しは1日数回・数KB程度のため無料枠内（実質0円運用）。

### public/data/forward/ の自動sync
- Day 7以降は forward-calibration.yml のsyncステップが自動更新する
- 次セッションで `/forward` 画面がDay 7以降のデータを表示していれば正常

---

## 重要ファイル・リンク

| 種別 | パス / URL |
|------|-----------|
| Webアプリ | https://trading-app-v2-94de8.web.app/forward |
| /forward 画面コード | `src/components/screens/ForwardCalibrationScreen.tsx` |
| データhook | `src/hooks/useForwardData.ts` |
| 実行スクリプト一式 | `hasebe201114-cyber/btc-carry-executor/scripts/` |
| 実行戦略パラメータ | `hasebe201114-cyber/btc-carry-executor/config/strategy-params.json` |
| フォワード仕様書 | `research/EXP-OBS000032/00-spec-forward-calibration.md` |
| 進捗信号機 | `research/ACTIVE.md` |
| 戦略ブリーフ | `research/STRATEGY-BRIEF.md` |
| PJ000004 設計書 | `obs/trading_app/80実装中/2026-07/OBS000032-自動売買システム概要設計.md` |

---

## 設計パラメータ（確定済み）

| パラメータ | 値 |
|-----------|-----|
| 初期資本 | 500 USDT |
| ドライラン期間 | 1ヶ月（〜2026-08-10） |
| 通知チャネル | Discord（DISCORD_WEBHOOK_DAILY） |
| 実行方式 | GitHub Actions 毎日自動（LIVE_MODE=false） |
| BTC w* | 3.629（ノーショナル = 500 × 3.629 = 1814 USDT） |
| ETH w* | 3.789（ノーショナル = 500 × 3.789 = 1895 USDT） |

---

## 前の引き継ぎ書
- `20260709-stage2-forward-calibration.md` → 完了済みへ移動
- `20260709-引き継ぎ.md` → 完了済みへ移動
