# 引き継ぎ書 2026-07-03

## 本日の成果サマリー

### 1. ローカル開発環境の立ち上げ（このPCで初回）
- GitHub リポジトリ（hasebe201114-cyber/trading-app-v2）をクローン
- `npm install` 済み。開発サーバーは `npm run dev` → http://localhost:5173

### 2. Firebase 接続の完了（Phase 0 の残作業を消化）
- Firebase プロジェクト `trading-app-v2-94de8` を作成し、`src/lib/firebase.ts` に設定値を反映
- Firebase Console で Google 認証を有効化 → ログイン動作確認済み
- Cloud Firestore API を有効化し、データベースを **asia-northeast1（東京）** に作成
- `firestore.rules` に `evaluationReports` コレクションを追加してデプロイ済み
- `.firebaserc` 追加（default = trading-app-v2-94de8）。Firebase CLI はこのPCでログイン済み

### 3. 評価レポートの Firestore 保存方式への移行（OBS000013・解決済み）
- `src/lib/evaluationReports.ts` 新規作成（保存・最新取得）
- 評価画面: Firestore から最新レポートを表示、未保存時は同梱JSONにフォールバック
- 「スナップショットをFirestoreへ保存」ボタン追加 → 実ブラウザで動作確認済み

### 本日のコミット（すべて main に push 済み）
| コミット | 内容 |
|---|---|
| `09afb92` | Firebase設定を実プロジェクトの値に置き換え |
| `0a46dbb` | 評価レポートをFirestore保存方式へ移行 |
| `4de12a9` | OBS000013を解決済へ移動 |

## プロジェクトの現在地
- ①〜⑦の全層が最低限動く状態（詳細: D001-開発計画.md）
- Phase 0（アプリ基盤）: **Firebase接続済みで実質完了**
- Phase 1（②パターン認識PoC）: 未達（複数期間フォールドで頑健性に課題、改善が必要）
- Phase 2（③LLM層）: プラミングのみ完成。**実LLM検証が未着手**
- Phase 3（④⑤）: パイプライン統合済み。最大DD目標達成、シャープレシオは直近期間で未達

## 明日のスタート地点（保留中の選択肢）

### 候補A: ③の実LLM化（推奨・Phase 2 KPI検証に必要）
- 現在 mock の LLM 特徴量抽出層（`src/llm-layer/`）を実際の Claude API 呼び出しに置き換える
- **必要なもの（ユーザ準備）**: Anthropic API キー、過去ニュースデータの調達方法の検討
- 目標: ②単体比でシャープレシオ +0.2 以上、または最大DD -3pt 以上の改善（PJ000001 5.2節）

### 候補B: 新規機能検討
- アイデアがあれば `01開発アイデア` に起票して着手

## 明日の起動手順
1. `npm run dev` → http://localhost:5173 → Google ログイン
2. Claude Code に「引き継ぎ書（obs/trading_app/引き継ぎ/20260703-引き継ぎ.md）を読んで続きから」と伝えれば再開できます

## 変更履歴
- 2026-07-03: 初版作成
